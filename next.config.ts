/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: "/stalink-pl",
  assetPrefix: "/stalink-pl/",
  output: "export", // <- DODAJ TO
};

module.exports = nextConfig;
